spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./data/blog
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: drop-and-create
    show-sql: false

logging:
  level:
    root: info